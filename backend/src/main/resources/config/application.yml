server:
  port: 8005
  servlet:
    context-path: /api

spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mariadb://localhost:3307/the_one
    username: root
    password: "07310731"
    driver-class-name: org.mariadb.jdbc.Driver
  thymeleaf:
    cache: false
